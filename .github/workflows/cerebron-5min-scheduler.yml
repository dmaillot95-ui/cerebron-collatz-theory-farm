name: CEREBRON Farm 01 5min Scheduler

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: cerebron-farm01-scheduler
  cancel-in-progress: true

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch theory farm
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run theory-farm.yml --repo "${{ github.repository }}" --ref main
